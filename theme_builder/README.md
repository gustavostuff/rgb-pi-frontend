# Theme builder

The **Tauri + Vite app** lives in **`app/`**. Node commands must be run from there:

```bash
cd app
npm install
npm run tauri dev
```

Bundled sample themes for development: **`themes/`**. Plan: **`BUILD_PLAN.md`**.
